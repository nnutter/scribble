{
  "name": "scribble",
  "version": "0.0.0",
  "private": true,
  "description": "A small Wails v3 desktop app for drawing a signature.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check frontend/app.js && node --check frontend/signature.js"
  }
}
